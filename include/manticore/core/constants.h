#pragma once

#include <cstdint>

namespace manticore {

constexpr uint32_t kDefaultSampleRateHz = 48000;
constexpr uint32_t kDefaultFrameSize = 64;
constexpr uint32_t kDefaultChannels = 1;
constexpr float kNormalizedMax = 1.0f;
constexpr float kNormalizedMin = -1.0f;

}  // namespace manticore
